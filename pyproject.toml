[project]
name = "food-calorie-backend"
version = "0.1.0"
description = "Food Calorie Estimation API with Flask and CNN"
requires-python = ">=3.12"

dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "numpy>=1.26.0",
    "pillow>=10.0.0",
    "requests>=2.31.0"
]